export interface Produto {
  id?: number;
  nome: string;
  preco: number;
  descricao: string;
  imagemUrl?: string;
}
export interface Cliente {
  id?: number;
  nome: string;
  email: string;
  telefone: string;
}
